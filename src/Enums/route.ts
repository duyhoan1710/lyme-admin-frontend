export enum EAppRoutes {
    HOME = "/",
    CATEGORY = "/categories",
    PRODUCT = "/products",
    ORDER = "/orders",
    EVALUATE = "/evaluates",
    SALES = "/sales",
    LOGIN = "/login",
    CHAT = "/chats",
    CHAT_DETAIL = "/chats/:chatType/:chatId",
}
